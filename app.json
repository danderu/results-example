{
    "name": "Slacker",
    "env": {
        "APPLICATION_URL": {
            "required": true,
            "value": "https://<your_app_name>.herokuapp.com",
            "description": "Application URL which will be used to construct a callback URL for OAuth mechanism"
        },
        "CLIENT_ID": {
            "required": true,
            "description": "The Client ID you've got from Slack"
        },
        "CLIENT_SECRET": {
            "required": true,
            "description": "The Client Secret you've got from Slack"
        }
    }
}
